# Gap: Slow Legacy Assets and Core Web Vitals

## Current state

- 15+ CSS/JS files per page
- jQuery + Bootstrap 3 + multiple plugins
- Google Fonts loaded via CSS `@import` (render-blocking)
- Large unoptimized images in `/images/`

## Impact

- Poor LCP on mobile networks in India
- SEO ranking factor (Core Web Vitals)

## Severity

**Medium–High**
